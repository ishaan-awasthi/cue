// Drag-and-drop file upload component. Accepts PDF, PPTX, DOCX, TXT, MD. On drop or file select,
// calls POST /files/upload via lib/api.ts. Shows upload progress, success state with chunk count,
// and error state. Used in /files page.
